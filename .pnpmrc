# Deduplicate peer dependencies to avoid multiple React instances
dedupe-peer-dependents=true

# Hoist React and related packages to avoid hook errors
public-hoist-pattern[]=*react*
public-hoist-pattern[]=*@testing-library/*
