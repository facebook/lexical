{
  "name": "lexical-link-source-mode",
  "private": true,
  "type": "module",
  "version": "0.44.0",
  "description": "Integration fixture: consume lexical via pnpm's link: protocol and the `source` export condition (TypeScript source, no build of lexical required). Source mode needs the monorepo's packages/shared/ reachable through the symlink, so this exercises pnpm link / pnpm add link:..., not file:.",
  "scripts": {
    "build": "vite build",
    "test": "node ./verify-bundle.mjs"
  },
  "dependencies": {
    "lexical": "link:../../../../../packages/lexical"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vite": "^7.3.2"
  },
  "optionalDependencies": {
    "@rollup/rollup-linux-x64-gnu": "4.52.0",
    "@rollup/rollup-darwin-arm64": "4.52.0",
    "@rollup/rollup-win32-x64-msvc": "4.52.0",
    "@rollup/wasm-node": "4.52.0"
  }
}
