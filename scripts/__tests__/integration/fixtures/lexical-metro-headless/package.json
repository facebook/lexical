{
  "name": "lexical-metro-headless",
  "private": true,
  "type": "commonjs",
  "version": "0.50.0",
  "description": "Integration fixture: bundle @lexical/headless and lexical with Metro (the React Native bundler) and execute the bundles. Covers Metro's package-exports resolution of the published ESM builds, both with Metro's defaults and with the documented resolveRequest that adds the development/production condition.",
  "scripts": {
    "build": "node ./bundle.js",
    "test": "node ./verify-bundles.js"
  },
  "dependencies": {
    "@lexical/headless": "0.50.0",
    "lexical": "0.50.0"
  },
  "devDependencies": {
    "@react-native/babel-preset": "^0.81.6",
    "metro": "^0.83.8",
    "metro-babel-transformer": "^0.83.8",
    "metro-config": "^0.83.8",
    "metro-runtime": "^0.83.8"
  }
}
