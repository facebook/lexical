/**
 * Copyright (c) Meta Platforms, Inc. and affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 * @flow strict
 */

/**
 * LexicalDevtoolsCore
 */

import type {
    LexicalEditor
} from 'lexical';

import {
  LexicalCommand,
} from 'lexical';

export type LexicalCommandLog = ReadonlyArray<
  LexicalCommand<any> & {payload: any}
>;

declare export function generateContent(
  editor: LexicalEditor,
  commandsLog: ReadonlyArray<LexicalCommand<any> & {payload: any}>,
  exportDOM: boolean,
  obfuscateText?: boolean,
): string;

declare export function registerLexicalCommandLogger(
  editor: LexicalEditor,
  setLoggedCommands: (
    v: (oldValue: LexicalCommandLog) => LexicalCommandLog,
  ) => void,
): () => void;

declare export function useLexicalCommandsLog(
  editor: LexicalEditor,
): LexicalCommandLog;